---
title: 'ERROR  child process failed, exited with error number 62'
date: '2020-04-19'
tags: ['MongoDB']
draft: false
summary: 2020 年 4 月 19 日，做数据库恢复后重启 mongodb 出现“ERROR: child process failed, exited with error number 62”错误，解决方法是删除 dbpath 所在目录（如 db 目录）并重新创建，若 db 目录不存在会报“ERROR: child process failed, exited with error number 100”错误。 
---

今天做了数据库恢复动作，然后重启mongodb的时候出现如下错误：
```
ERROR: child process failed, exited with error number 62
```

解决方法是：
找到dbpath所在的目录删除掉（注意你之前的数据应该有备份），如我的存放在db目录
```
rm -rf ./db
mkdir db
```
然后重新创建db目录，再启动就可以了。

注意，如果db目录不存在会报如下错误：
```
ERROR: child process failed, exited with error number 100
```