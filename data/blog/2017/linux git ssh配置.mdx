---
title: 'linux git ssh配置'
date: '2017-01-01'
tags: ['Tools']
draft: false
summary: 
---

- git config —global user.name “tujiaw”  
- git config —global user.email “tujiaw@163.com”
- cd ~/.ssh
- ssh-keygen -t rsa -C “tujiaw@163.com”
- 直接按回车，目录下会产品两个文件：id_rsa和id_rsa.pub

打开id_rsa.pub文件  
vi id_rsa.pub   
拷贝里面的全部内容添加到github中  
登录github打开profile中的setting->SSH and GPG keys->New SSH key粘贴到key中  
验证：
> ssh -T git@github.com  

最后就可以用git clone命令了